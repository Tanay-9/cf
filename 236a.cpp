#include<bits/stdc++.h>
using namespace std;

void solve() {
    string s;
    cin >> s;

    unordered_set<char>st;
    for(auto it : s) {
        st.insert(it);
    }

    if (st.size()%2 == 0) 
     "CHAT WITH HER!" 
      "IGNORE HIM!";
}

int main() {
    solve();

    return 0;
}